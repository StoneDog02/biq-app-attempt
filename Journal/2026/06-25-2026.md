# 06/25/2026

## Nav UX — 5-tab restructure + Journal rollover
- Reduced tab bar from 6 to 5 tabs: Care hidden (`href: null`); routes unchanged
- Profile: **Care & wellness** hub (supplement check-in, shop, portal, telehealth, labs)
- Today's Plan: **Log supplements** link on Supplements section → hidden care screen
- Care screen: back affordance to Profile; `app/care/_layout.tsx` back fallback → Profile
- Files: `app/(tabs)/_layout.tsx`, `app/(tabs)/profile.tsx`, `app/(tabs)/today.tsx`, `app/(tabs)/care.tsx`, `app/care/_layout.tsx`, `docs/plans/phase-4-care.md`

## Journal — automatic daily rollover
- Created `Journal/2026/06-25-2026.md` (06/24 left unchanged)
- `sessionStart` hook: `.cursor/hooks/ensure-journal-today.sh` creates today's file if missing
- `.cursor/hooks.json` registers hook; script made executable
- `bodyiq-journal.mdc`: `alwaysApply: true` + session-start date check rules
- `bodyiq-plans.mdc`: cross-ref to append to today's Journal file only
- Updated `Journal/README.md` with rollover documentation

## Today's Plan loop polish (implemented)
- `ensureTodayPlan` auto-seeds fresh `daily_plans` row on new calendar day
- `planCoach.ts` — weekday greeting + engagement tone + goal-aware coach copy
- `planProgress.ts` — day progress bar (`X of Y done`)
- Today screen: action links (habits, movement, meals, supplements), checkmarks on completed cards, pull-to-refresh, refetch on tab focus
- Files: `src/services/planRefresh.ts`, `planCoach.ts`, `planProgress.ts`, `app/(tabs)/today.tsx`, `src/hooks/usePlan.ts`

## Today's Plan — segmented layout (implemented)
- Segment bar (Habits | Movement | Meals | Supplements) — one domain panel at a time, less scrolling
- `planSectionOrder.ts` — goal-based segment order, domain summaries, auto-select + auto-advance
- Components: `DomainSegmentBar`, `PlanSection` in `src/components/today/`
- Files: `src/services/planSectionOrder.ts`, `app/(tabs)/today.tsx`

## Habits tab polish (implemented)
- Pinned header: HABITS label, "Today's habits" title, copper day progress bar (`habitProgress.ts`)
- Shared `DayProgressBar` extracted to `src/components/ui/`; Today imports unchanged
- Habit cards: SymbolView checkmarks, cardDone styling; light vs moderate tier UX preserved
- History: 7-day rows with copper dot indicators (`HabitHistoryDots`)
- Pull-to-refresh + `useFocusEffect` query invalidation on Habits tab
- Files: `src/services/habitProgress.ts`, `src/components/ui/DayProgressBar.tsx`, `src/components/habits/HabitHistoryDots.tsx`, `app/(tabs)/habits.tsx`, `docs/plans/phase-habits-polish.md`

## Habits UI elevation (implemented)
- Grouped checklist replaces 3 separate Cards — compact rows with icons, dividers, copper done accent
- Per-habit spinner via `logHabit.variables.habitType`; removed global Save buttons
- Water stepper auto-saves on +/-; Steps tap-to-complete for moderate tier
- Optimistic cache updates in `useLogHabit`
- Files: `src/components/habits/HabitRow.tsx`, `HabitChecklist.tsx`, `HabitStepper.tsx`, `app/(tabs)/habits.tsx`, `src/hooks/usePlan.ts`

## Nutrition tab elevation (implemented)
- Pinned header: NUTRITION label, "Today's meals" title, copper day progress bar (`nutritionProgress.ts`)
- Level 1: grouped check-in panel (On plan / Mostly / Off plan) with per-row loading
- Level 2+: compact action chips, grouped meal log panel, compact meal list rows
- Pull-to-refresh + focus refetch; optimistic check-in + meal log in `useNutrition`
- Files: `src/services/nutritionProgress.ts`, `src/components/nutrition/*`, `app/(tabs)/nutrition.tsx`, `docs/plans/phase-nutrition-polish.md`

## Nutrition plan builder (implemented)
- `nutrition_profile` JSONB: weight, goal weight, meals/day, protein/carb/fat favorites
- `macroCalculator.ts` + meal planner v2 with named meals and macro lines
- Nutrition Setup mini-flow at `/nutrition/setup` for existing users
- Onboarding: body_metrics, macro_foods, meal_schedule steps
- Nutrition tab: MacroSummaryCard, TodayMealPlanPanel, setup banner
- Migration applied: `20260625160000_nutrition_profile.sql`
- Files: `src/services/macroCalculator.ts`, `src/services/mealPlanner.ts`, `app/nutrition/setup/*`, `docs/plans/phase-nutrition-plan-builder.md`
